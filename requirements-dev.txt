ruff>=0.6
pytest>=8.0
pytest-asyncio>=0.24
# TestClient из starlette работает поверх httpx, но сам его не тянет. Локально он
# приезжает попутно с другими пакетами, а на чистой машине CI — нет.
httpx>=0.27
