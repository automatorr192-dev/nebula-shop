"""Пустой файл, но нужный: по нему pytest понимает, где корень проекта, и добавляет
его в пути импорта. Без этого тест из tests/ не видит модули из корня и падает на
первой же строке `from quiz import QUIZ`.
"""
